apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kubernetes-config
  namespace: argocd     
spec:
  project: core-tools-config
  source:
    repoURL: https://github.com/ndawpa/guiadodevops
    path: kubernetes/argocd/config/core-tools/kubernetes-config
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: operational
  syncPolicy:
    automated:
      prune: true
      allowEmpty: true 
      selfHeal: true
